---
import { Image } from 'astro:assets';
import { expertisesRhBackground, rhDonuts } from '../../../data/expertises';

const center = 117;
const radius = 100;
const labelRadius = radius;
const circumference = 2 * Math.PI * radius;
const coloredShare = 0.71326;
const coloredArcLength = circumference * coloredShare;

const polarToCartesian = (angle: number) => {
  const radians = (angle - 90) * (Math.PI / 180);
  return {
    x: center + labelRadius * Math.cos(radians),
    y: center + labelRadius * Math.sin(radians),
  };
};

const rightLabel = polarToCartesian(90);
const leftLabel = polarToCartesian(240);
---

<section class="text-bleu-abysse">
  <div class="relative mx-auto w-full max-w-[1440px] overflow-hidden bg-[#062f4b] md:h-[1212px]">
    <Image
      src={expertisesRhBackground}
      alt="Collaborateurs Reunimer"
      class="absolute inset-0 h-full w-full object-cover object-right-bottom"
      widths={[768, 1280, 1600, 2000]}
      sizes="100vw"
    />
    <div class="relative z-10 min-h-[760px] px-7 pb-14 pt-12 md:min-h-[1212px] md:px-0 md:pb-0 md:pt-0">
      <div class="max-w-[min(33rem,100%)]">
        <h2 class="font-display text-[24px] font-black uppercase leading-[0.95] tracking-[-0.02em] text-ecume-poudree md:absolute md:left-[87px] md:top-[115px] md:w-[934px] md:text-[40px] md:leading-[44.5px]">
          Notre politique
          <br />
          ressources humaines
        </h2>
        <p class="mt-4 max-w-[26rem] font-heading text-[14px] leading-[1.45] text-[#002c41] md:absolute md:left-[87px] md:top-[224px] md:mt-0 md:max-w-none md:text-[20px] md:leading-[1.461]">
          Petit texte d’introduction sur la politique RH.
        </p>
      </div>

      <div class="mt-12 max-w-[34rem] md:absolute md:left-[127px] md:top-[444px] md:mt-0 md:max-w-none">
        <p class="font-heading text-[18px] font-semibold leading-[1.25] text-white md:w-[471px] md:text-[20px] md:leading-[normal]">
          Une marque employeur fondée sur le talent et le collectif fort de 1013 collaborateurs
        </p>
        <p class="mt-4 font-body text-[13px] leading-[1.45] text-white/92 md:mt-[17px] md:w-[562px] md:text-[16px] md:leading-[1.4]">
          Le Groupe cultive une force de travail animée par l’esprit d’ensemble. Notre ambition RH est de placer la
          bienveillance et l’éthique au coeur de nos métiers. En valorisant chaque talent et en favorisant la
          transmission des savoir-faire aux générations futures, Reunimer s’affirme comme un employeur de référence en
          Océan Indien, garantissant un environnement de travail intègre, inclusif et stimulant.
        </p>
      </div>

      <div class="mt-10 max-w-[46rem] md:absolute md:left-[142px] md:top-[712px] md:mt-0">
        <div class="max-w-[44.5rem]">
          <h3 class="font-heading text-[18px] font-semibold leading-[1.2] text-white md:text-[20px]">
            La même chance pour tous
          </h3>
          <div class="mt-4 h-px w-full max-w-[522px] bg-white/75 md:mt-[14px]"></div>
        </div>

        <div class="mt-8 flex flex-wrap gap-8 md:mt-[62px] md:gap-[67px]">
          {rhDonuts.map((donut) => (
            <div class="relative h-[160px] w-[160px] md:h-[234px] md:w-[234px]">
              <svg viewBox="0 0 234 234" class="h-full w-full">
                <circle cx="117" cy="117" r={radius} fill="none" stroke="#FFFFFF" stroke-width="33.314" />
                <circle
                  cx="117"
                  cy="117"
                  r={radius}
                  fill="none"
                  stroke="#6EAEB5"
                  stroke-width="33.314"
                  stroke-dasharray={`${coloredArcLength} ${circumference}`}
                  stroke-dashoffset={String(coloredArcLength / 2)}
                  transform="rotate(-90 117 117)"
                />
                <text
                  x={rightLabel.x}
                  y={rightLabel.y}
                  text-anchor="middle"
                  dominant-baseline="middle"
                  class="font-body text-[11px] font-bold leading-none fill-white md:text-[16px]"
                >
                  {donut.rightValue}
                </text>
                <text
                  x={leftLabel.x}
                  y={leftLabel.y}
                  text-anchor="middle"
                  dominant-baseline="middle"
                  class="font-body text-[11px] font-bold leading-none fill-bleu-abysse md:text-[16px]"
                >
                  {donut.leftValue}
                </text>
              </svg>
              <div class="absolute inset-[26%] flex flex-col items-center justify-center text-center">
                {donut.lines.map((line) => (
                  <p class="font-body text-[10px] font-bold leading-[1.08] text-white md:text-[16px]">
                    {line}
                  </p>
                ))}
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
