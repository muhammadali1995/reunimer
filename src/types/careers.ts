export interface JobListing {
  title: string;
  bgColor: string;
  missionText: string;
  benefitTitle: string;
  benefitText: string;
  extraTitle: string;
  extraText: string;
  defaultExpanded?: boolean;
}
